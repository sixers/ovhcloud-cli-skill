---
name: "recipe-inspect-webhosting"
description: "Inspect OVHcloud webhosting services, databases, runtimes, tasks, and deployments with read-only commands."
metadata:
  category: "recipe"
---

> Auto-generated by `python3 scripts/generate_bundle.py`. Do not edit manually.

# recipe-inspect-webhosting

## Goal

Inspect webhosting state, databases, runtimes, incidents, and tasks safely.

## Related Skills

- [ovhcloud-shared](../../subskills/shared/SKILL.md)
- [ovhcloud-webhosting](../../subskills/webhosting/SKILL.md)
- [ovhcloud-hosting-private-database](../../subskills/hosting-private-database/SKILL.md)

## Workflow

1. List webhosting services and fetch the specific service record you care about.
2. Inspect attached databases, runtimes, websites, incidents, and tasks through read-only verbs.
3. Document mutating operations such as `edit`, `terminate`, or `restore-snapshot` separately without executing them.

## Command Skeletons

- `ovhcloud webhosting list -o json`
- `ovhcloud webhosting get <service_name> -o json`
- `ovhcloud webhosting db list <service_name> -o json`
- `ovhcloud webhosting runtime get <service_name> <runtime_id> -o json`
- `ovhcloud webhosting tasks list <service_name> -o json`

## Safety

- Keep the workflow read-only unless the user explicitly requests a state change.
- If a later step would require a mutating verb, stop and confirm that separately instead of continuing by default.
