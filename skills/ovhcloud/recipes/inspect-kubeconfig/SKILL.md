---
name: "recipe-inspect-kubeconfig"
description: "Generate kubeconfig for cluster inspection only, without changing cluster state."
metadata:
  category: "recipe"
---

> Auto-generated by `python3 scripts/generate_bundle.py`. Do not edit manually.

# recipe-inspect-kubeconfig

## Goal

Generate kubeconfig locally so cluster inspection can continue outside the CLI.

## Related Skills

- [ovhcloud-shared](../subskills/shared/SKILL.md)
- [ovhcloud-cloud-kube](../subskills/cloud-kube/SKILL.md)

## Workflow

1. List clusters and fetch the target cluster record first.
2. Generate kubeconfig only after the cluster identifier is confirmed.
3. Treat kubeconfig generation as a local-file step, then continue inspection with `kubectl` outside this bundle if needed.

## Command Skeletons

- `ovhcloud cloud kube list --cloud-project <project_id> -o json`
- `ovhcloud cloud kube get --cloud-project <project_id> <cluster_id> -o json`
- `ovhcloud cloud kube kubeconfig generate --cloud-project <project_id> <cluster_id>`

## Safety

- Keep the workflow read-only unless the user explicitly requests a state change.
- If a later step would require a mutating verb, stop and confirm that separately instead of continuing by default.
