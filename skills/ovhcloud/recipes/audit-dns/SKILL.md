---
name: "recipe-audit-dns"
description: "Inspect domains, zones, and records safely with OVHcloud CLI DNS commands."
metadata:
  category: "recipe"
---

> Auto-generated by `python3 scripts/generate_bundle.py`. Do not edit manually.

# recipe-audit-dns

## Goal

Review domains, DNS zones, and records without refreshing or editing them.

## Related Skills

- [ovhcloud-shared](../../subskills/shared/SKILL.md)
- [ovhcloud-domain-name](../../subskills/domain-name/SKILL.md)
- [ovhcloud-domain-zone](../../subskills/domain-zone/SKILL.md)

## Workflow

1. List zones or domain names first, then fetch the specific zone you need.
2. List records for the zone and capture IDs before any deeper analysis.
3. Stop before `refresh`, `create`, `edit`, or `delete` operations unless the user later approves them.

## Command Skeletons

- `ovhcloud domain-zone list -o json`
- `ovhcloud domain-zone get <zone_name> -o json`
- `ovhcloud domain-zone record list <zone_name> -o json`
- `ovhcloud domain-name list -o json`

## Safety

- Keep the workflow read-only unless the user explicitly requests a state change.
- If a later step would require a mutating verb, stop and confirm that separately instead of continuing by default.
