interface:
  display_name: "Recipe Inventory Account"
  short_description: "Inventory an account, projects, and core services with read-o..."
  brand_color: "#0050D7"
  default_prompt: "Use $recipe-inventory-account to inventory an account, projects, and core services with read-only `ovhcloud` commands."
